#!/bin/bash -e

# Prepare the database when starting the app (Foreman or Thruster + Puma).
if [ "${1}" == "./bin/server" ] || \
   { [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; }; then
  ./bin/rails db:prepare
fi

exec "${@}"
